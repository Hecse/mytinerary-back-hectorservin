const controller = {
    getUsers: (req, res) => {
        res.json({
            user: 'Hector Servin'
        });
    },

    postUser: () => {},
    deletetUser: () => {},
}

export default controller;